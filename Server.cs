using System;

namespace BF2GamespyLoginEmulator
{
    class Server
    {
        public Server()
        {
            // Init the socket classes here

            // Main program loop
            while (true) 
            { 
                // Server is running.
                // Should allow for input commands
            }
        }
    }
}
